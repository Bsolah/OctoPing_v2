{
  "name": "@nova/gateway",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "node ./scripts/run-dev.mjs",
    "build": "tsc --noEmit",
    "vercel-build": "vercel build",
    "deploy": "vercel deploy --prod",
    "lint": "eslint . --ext .ts --ignore-pattern .vercel",
    "typecheck": "tsc --noEmit",
    "test": "tsx src/lib/gateway.test.ts"
  },
  "dependencies": {
    "@upstash/redis": "^1.34.3"
  },
  "devDependencies": {
    "@types/node": "^20.16.5",
    "tsx": "^4.19.1",
    "typescript": "^5.6.2",
    "vercel": "^54.20.1"
  }
}
